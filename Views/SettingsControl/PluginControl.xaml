<UserControl
    x:Class="ProgramManager.Views.SettingsControl.PluginControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ProgramManager.ViewModels.Settings"
    Height="647.561"
    d:DesignWidth="550"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <vm:PluginViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="100" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBox
            Margin="5"
            FontSize="14"
            Text="Поиск" />
        <ListBox
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Margin="10"
            ItemsSource="{Binding PluginList, Mode=OneWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="400" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>
                        <Image
                            Height="32"
                            Margin="10,0"
                            Source="../../Resources/Icons/Aard.png" />
                        <StackPanel Grid.Column="1">
                            <TextBlock
                                Margin="2"
                                FontSize="12"
                                FontWeight="Bold"
                                Text="{Binding Name, Mode=OneWay}" />
                            <TextBlock
                                Margin="2"
                                FontSize="10"
                                Text="{Binding Author, Mode=OneWay}" />
                            <TextBlock
                                Height="25"
                                Margin="2"
                                FontSize="10"
                                Text="{Binding Description, Mode=OneWay}" />
                        </StackPanel>
                        <Button
                            Grid.Column="2"
                            Width="25"
                            Height="25"
                            Command="{Binding Execute, Mode=Default}" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
